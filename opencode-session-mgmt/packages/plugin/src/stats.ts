/**
 * 本机统计聚合查询（设计文档 §4.3、§6）。
 * 会话级/项目级：插件库（工作流/会话内质量）+ 上游 session SDK（cost/tokens），
 * 按 sessionID 关联。供 opencode-sm 复用。
 */

// TODO: sessionStats(sessionID)
// TODO: projectStats(project, period)
