---
stage: prd
---
# reqdoc 可验证性与验收标准规约

## 1. 每条需求可验证

功能点须附验收标准（前置→操作→预期结果）；无法验证视为不完整。验收至少覆盖三类：功能验收、非功能验收（性能/并发/可用性/安全，须给可测指标）、监管合规验收（报送/审计/权限隔离等要求，须落到具体条款与可验证动作）。"满足监管要求"本身不可测，须拆到具体条款 + 可验证动作。

## 2. 禁用不可测形容词

避免「用户友好/高效/稳定」等，改为可观测/可量化条件。

## 3. 验收可独立判定

验收标准应能被人工或自动化独立判定通过/不通过。
