/**
 * opencode-sm init —— 每台机器一次的自助配置（设计文档 §5.1）。
 * 交互式四问：账号 / 组 / 组织 / 收集服务地址，写入全局
 * ~/.config/opencode/session-mgmt/identity.json（sm-shared 的 writeIdentity）。
 */

// TODO: prompts 四问 + 写入 + 回显确认
