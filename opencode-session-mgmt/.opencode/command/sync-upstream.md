---
description: 按 docs/upstream-sync.md 手工同步上游
---

严格按 `docs/upstream-sync.md` 执行上游同步，先展示完整执行计划再动手：

- remote 布局：`origin`=wonglaitung/opencode（日常 `git pull` 用），`upstream`=anomalyco/opencode 且 push 已禁用。
- 冲突预案：若与上游冲突，按文档步骤处理，不得擅自改写上游文件。
- 同步后按文档「同步记录」要求补记。

不要自行发明同步步骤；任何与文档不一致的取舍先向用户确认。
