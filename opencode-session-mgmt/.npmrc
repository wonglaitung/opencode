# 使用 hoisted 模式安装依赖：所有包以真实目录/文件形式存在于 node_modules 中，
# 无符号链接、无硬链接引用外部存储。这使得整个目录可 tar/zip 打包后在任意机器解压即用。
#
# 背景：bun 默认使用 isolated 链接策略，在 Windows 上通过硬链接从全局缓存引用包文件，
# 打包时硬链接无法跨机器保留，导致传递依赖（如 zod）断裂、插件加载失败。
node-linker=hoisted
